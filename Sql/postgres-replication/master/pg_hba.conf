# Local connections
local   all             all                                     trust

# IPv4 localhost
host    all             all             127.0.0.1/32            trust

# IPv6 localhost
host    all             all             ::1/128                 trust

# Allow normal client connections
host    all             all             all                     scram-sha-256

# Allow replication connections
host    replication     replicator      all                     scram-sha-256